#include<iostream>
using namespace std;
int main()
{
    int n;
    cout<<"enter n:";
    cin>>n;
    // ternary operator me if not uses only condition used
    // (condition) ? if true : if false ;

    (n%2==0) ? cout<<"even number":  cout<<"odd number";
    
    
}